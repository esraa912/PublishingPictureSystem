package com.pioneers.picturepublishingservice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the Picture Publishing Service Spring Boot application.
 *
 * @author esraa
 */
@SpringBootApplication
public class PicturePublishingServiceApplication {

    /**
     * Main method that launches the Spring Boot application.
     *
     * @param args command-line arguments passed to the application
     */
    public static void main(String[] args) {
        SpringApplication.run(PicturePublishingServiceApplication.class, args);
    }
}
